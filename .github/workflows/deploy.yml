name: Deploy

on:
  push:
    branches:
      - main

permissions:
  actions: read
  contents: read

jobs:
  build-affected:
    name: Build affected projects
    runs-on: ubuntu-latest

    env:
      NX_DAEMON: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set SHAs for affected commands
        uses: nrwl/nx-set-shas@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Get affected projects
        id: affected
        shell: bash
        run: |
          echo "NX_BASE=$NX_BASE"
          echo "NX_HEAD=$NX_HEAD"

          PROJECTS=$(npx nx show projects \
            --affected \
            --base=$NX_BASE \
            --head=$NX_HEAD \
            --plain)

          echo ""
          echo "Affected projects:"
          echo "$PROJECTS"

          {
            echo 'projects<<EOF'
            echo "$PROJECTS"
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Build affected projects individually
        shell: bash
        run: |
          while IFS= read -r project
          do
            if [ -n "$project" ]; then
              echo ""
              echo "=============================="
              echo "Building project: $project"
              echo "=============================="

              npx nx build "$project" \
                --configuration=production
            fi
          done <<< "${{ steps.affected.outputs.projects }}"

      - name: Upload artifact - shell
        uses: actions/upload-artifact@v4
        with:
          name: shell
          path: apps/shell/dist
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload artifact - product
        uses: actions/upload-artifact@v4
        with:
          name: product
          path: apps/product/dist
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload artifact - pc_builder
        uses: actions/upload-artifact@v4
        with:
          name: pc_builder
          path: apps/pc_builder/dist
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload artifact - manager_cx
        uses: actions/upload-artifact@v4
        with:
          name: manager_cx
          path: apps/manager_cx/dist
          if-no-files-found: ignore
          retention-days: 7
