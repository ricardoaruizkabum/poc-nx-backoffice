# Remote URLs de produção para Module Federation (shell app)
# Estas URLs são públicas (acessíveis por qualquer usuário do site) e não são segredos.
# Substituir pelos valores reais ao configurar o ambiente de produção.

PRODUCT_REMOTE_URL=https://cdn.example.com/product
PC_BUILDER_REMOTE_URL=https://cdn.example.com/pc_builder
MANAGER_CX_REMOTE_URL=https://cdn.example.com/manager_cx
