# Remote URLs for Module Federation (shell app)
# These values are used at build/serve time by apps/shell/module-federation.config.ts
# Override locally via .env.local (gitignored)

PRODUCT_REMOTE_URL=http://localhost:4210
PC_BUILDER_REMOTE_URL=http://localhost:4211
MANAGER_CX_REMOTE_URL=http://localhost:4212
